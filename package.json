{
  "name": "esbuild",
  "version": "1.0.0",
  "description": "esbuild",
  "main": "index.js",
  "type": "module",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "esbuild": "npx esbuild app.jsx  --bundle --outfile=out.js --loader:.js=jsx --minify"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "dependencies": {
    "esbuild": "^0.19.2",
    "react": "^18.2.0",
    "react-dom": "^18.2.0"
  }
}
